// MCP Server - Phase 5
// Model Context Protocol server for agent integration

const port = Number(process.env.MCP_PORT) || 3002

console.log(`repogen mcp server will start on port ${port} (Phase 5)`)
